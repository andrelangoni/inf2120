Cours 11
tri rapide ou quicksort
récursif : diviser le tableau en deux
-pivot
-partition
-appel tri 2 sous-tableaux
-arrêt lorsque le sous-tableau est de taille 2
-ex.: pivot (choix de la valeur) = premier + dernier / 2 ou valeur aléatoire 
-autre technique : choix de la position
-moyen : O(n log(n))
-pire : O(n^2)

interfaces graphiques custom
panneau : awt - canvas
swing - Jpanel
- méthode paint()

public class BB extends JPanel {
    @Override
    public void paint(Graphics g) {
        g.draw();
    }
}

MouseListener

Révision Final - pas récapitulatif
cours 7 à 11
cours 7
classes Function : Supplier, Consumer, ...
notation lambda,
x -> x + 1 (fonction)
stream se sert des fonctions
filter, sorted, map, foreach, iterate
pas de routine pour nombres premiers

cours 8
GUI : Swing - Composante, contenant, listener
JTextField, JLabel, JButton, JPanel, JFrame, ...
Layout : BorderLayout, GridLayout, FlowLayout
Listener, Event,

cours 9
cours 9 et 10
comparaison avec equals(), compareTo(),type extends Comparable

connaître l'ordre associé à chaque algorithme
type, complexité/ordre temporelle
connaître l'algorithme pour les 2 types de recherches
recherche linéaire O(n),
recherche binaire O(log(n))

arbre binaire de recherche
fouille*
insertion*
suppression*
* ordre O(log(n))
parcours infixe, préfixe, (suffixe ou postfixe)

cours 10
tri (compareTo())
- code
- trace
- partition
meilleur = 1
moyen = 2
pire = 3
sélection
1 = n^2, 2 = n^2, 3 = n^2
insertion
n, n^2 ,n^2
bulle
n, n^2 ,n^2
tri rapide (quicksort)
n log(n), n log(n), n^2

